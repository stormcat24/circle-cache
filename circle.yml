machine:
  timezone: Asia/Tokyo

dependencies:
  cache_directories:
    - ~/advent

  override:
    - echo hogehoge
    - echo "`date +'%Y-%m-%d %H:%M:%S'` branch=$CIRCLE_BRANCH build_number=$CIRCLE_BUILD_NUM" >> ~/advent/build.log

test:
  override:
    - echo testtest!
